---
name: "geekbi-temu-shop-search-skill"
description: "通过极鲸云实时搜索和分析 Temu 店铺，支持按店铺名称或 ID、站点、可信类目 ID、托管模式、销量、销售额、评分、评论数、商品数、粉丝数、价格、开店时间、增长和动销指标筛选及排序。用户提到 Temu 店铺搜索、店铺链接、竞品店铺、竞店调研、热销店铺、头部店铺、新店、高成长店铺、店铺经营规模、动销效率或店铺排名时使用。"
---

# 极鲸云 Temu 店铺搜索

仅使用极鲸云实时返回的数据搜索和分析 Temu 店铺。将结论限定在店铺层级，不编造店铺、指标、链接或未返回的经营信息。

## 执行流程

1. 先读取 [Temu 店铺搜索](references/Temu店铺搜索.md)，按其中的业务口径、分析方法和输出要求执行。
2. 读取 [Temu 店铺搜索接口](references/Temu店铺搜索接口.md)，只选取用户请求所需的查询参数。
3. 未指定站点或指定美国站时使用站点 ID `48`。指定其他国家或地区时，先运行 `python3 scripts/temu_site_list.py --country "国家或地区名"`，只采用唯一精确匹配的站点 ID。
4. 仅在已有可信类目 ID 时传入 `catIds`。没有可信 ID 时不得按类目名称猜测；优先省略类目条件并按店铺关键词搜索，类目条件不可省略时请用户提供明确 ID。
5. 用户请求热销店铺、热销新店、最近开店等榜单时，再读取 [Temu 店铺榜单查询预设](references/Temu店铺榜单预设.md)，每次只采用一个最符合意图的主预设。
6. 从本 Skill 根目录运行 `python3 scripts/temu_shop_search.py --param "名称=值"`。需要多个条件时重复传入 `--param`；需要多个类目 ID 时重复传入 `catIds`。
7. 检查返回结构、命中总数、当前页、样本量和更新时间。需要全量结果时逐页查询；未完成分页时明确说明仅为样本。
8. 按参考文档分析数据并输出中文业务结论。对每个返回非空 `linkUrl` 的店铺，将店铺名称写成可点击链接。

## 复用登录态

- 先直接运行店铺搜索脚本。脚本通过 `scripts/geekbi_auth.py` 优先读取所有极鲸云 Temu Skills 共用的系统用户配置登录态；已有有效登录态时不得再次要求登录。
- 仅在脚本返回 `actionRequired` 或退出码 `2` 时读取并执行 [查询暂停与恢复流程](references/查询暂停与恢复流程.md)。向用户展示服务端中文提示和有效跳转链接，然后暂停查询；用户完成操作并回复继续后，原样重跑同一命令。
- 退出码 `1` 时仅展示错误输出中的一级中文 `msg`。不得展示访问令牌、设备码、请求头、原始错误 JSON 或异常栈。

## 资源路由

- 执行任何店铺搜索或店铺分析时，读取 `references/Temu店铺搜索.md`。
- 组装查询参数、选择排序字段或解释返回字段时，读取 `references/Temu店铺搜索接口.md`。
- 处理店铺榜单请求时，读取 `references/Temu店铺榜单预设.md`。
- 仅在服务端要求用户操作时，读取 `references/查询暂停与恢复流程.md`。

若环境尚未安装依赖，运行 `python3 -m pip install -r requirements.txt` 后再执行查询脚本。
